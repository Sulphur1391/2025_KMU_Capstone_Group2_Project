services:
  - type: web
    name: fashion-ai-clothes-analyzer
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:10000 --workers 2"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"